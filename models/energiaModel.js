class CasoEnergia {
    constructor(id, caso, local, descricao, impacto) {
        this.id = id;
        this.caso = caso;
        this.local = local;
        this.descricao = descricao;
        this.impacto = impacto;
    }
}

module.exports = CasoEnergia;